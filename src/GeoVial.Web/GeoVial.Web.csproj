<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\GeoVial.Shared\GeoVial.Shared.csproj" />
    <!-- VistaMapa: cálculo de la vista del mapa interactivo de la revisión (US-21), núcleo en el gate. -->
    <ProjectReference Include="..\GeoVial.Revision\GeoVial.Revision.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <BlazorDisableThrowNavigationException>true</BlazorDisableThrowNavigationException>
  </PropertyGroup>

</Project>
